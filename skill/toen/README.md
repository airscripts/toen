# Toen Skill

This is the host-neutral Toen skill. Upload or copy `SKILL.md` into an
assistant that supports Markdown skills or custom instructions, then invoke it
explicitly with `$toen ammodino` or `$toen arranda`.

The skill changes visible replies, status updates, and tool narration only.
Technical terms, protected literals, and requested deliverables remain exact.
